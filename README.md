A small Python script that converts a movie title into a Letterboxd URL.
Also performs a live HTTP check to ensure the film actually exists on Letterboxd, so no invalid links are returned.

Install dependencies:

```bash
pip install requests
